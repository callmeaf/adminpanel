"use client";

import * as React from "react";
import DashboardLayout from "@/modules/Dashboard/widgets/DashboardLayout";
import {{plurarModuleName}}Wrapper from "../widgets/{{plurarModuleName}}Wrapper";
import moduleConfig from "../module.config";
import usePage from "@/modules/Base/hooks/use-page";

interface I{{plurarModuleName}}TrashedPageProps {}

const {{plurarModuleName}}TrashedPage: React.FC<I{{plurarModuleName}}TrashedPageProps> = ({}) => {
  usePage(moduleConfig);

  return (
    <DashboardLayout>
      <{{plurarModuleName}}Wrapper trashed />
    </DashboardLayout>
  );
};

export default {{plurarModuleName}}TrashedPage;
